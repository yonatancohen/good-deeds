# Use public npm registry for this project (bypasses Fireblocks JFrog)
registry=https://registry.npmjs.org/
# Only install package versions that are at least 21 days old.
# Gives time for hot-off-the-press regressions to surface before we ship.
min-release-age=21
legacy-peer-deps=true
